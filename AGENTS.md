# YouTube 영상 제작 자동화 작업 기준

## 범위

사용자 아이디어 → 전체 대본 → 장면 분리 → 장면별 영어 Flow/Veo 프롬프트 → Google Flow 영상 생성 → 프로젝트에 파일 저장.

음악, 자막, 음성 합성, 최종 영상 편집, DaVinci Resolve 작업, YouTube 업로드는 실행하지 않는다.

## 계정 및 실제 옵션 확인

- 먼저 `flow_list_accounts`로 기본 계정이 connected 상태인지 확인한다.
- 연결된 계정이 있으면 불필요하게 다시 연결하지 않는다.
- `flow_inspect_account`로 실제 영상 모델, 미디어별 화면 비율, 선택 가능한 길이, 출력 개수 등을 확인한다. 모델·길이·크레딧 비용을 추측하지 않는다.
- 길이 선택 옵션이 노출되지 않으면 그 사실을 알리고 `durationSeconds`를 임의로 전달하지 않는다. 생성 전 길이를 확정할 수 없는 경우 이를 사용자에게 명시한다.
- Google/Veo API 키를 사용하지 않는다. 기존 Google Flow 구독과 크레딧 경로만 사용한다.
- Flow 웹사이트 작업은 Google Flow MCP 전용 도구로 수행한다. 일반 브라우저 자동화로 대체하지 않는다.
- 계정 연결이 필요하면 `flow_begin_account_connection`을 호출하고 반환된 안내를 사용자에게 보여준 뒤 멈춘다. 사용자가 확장에 `Session sent`가 표시됐다고 확인한 뒤에만 정확한 connectionId로 `flow_complete_account_connection`을 호출한다. 직전에 임시 Google 계정 선택 창에서 기존 계정을 직접 선택하도록 안내한다.
- 사용자의 기본 브라우저는 웨일이다. Flow Login Bridge는 기존 Google 계정이 로그인된 사용자 브라우저에 설치한다.

## 1. 전체 대본 승인

- 사용자가 주제를 제공하면 듣기 편한 YouTube 말투로 전체 대본을 작성한다.
- 어려운 표현과 불필요한 반복을 줄이고, 처음 10~20초에 관심을 끌며 장면으로 만들기 좋은 구조를 사용한다.
- 전체 대본을 보여주고 수정을 받는다. 사용자가 승인하기 전에는 다음 단계로 넘어가거나 영상을 생성하지 않는다.

## 2. 장면 분리 및 프롬프트

승인된 대본을 장면으로 나누고 각 장면에 다음을 작성한다.

- Scene 번호
- 내레이션
- 장면 설명
- 카메라 움직임
- 등장인물 또는 주요 사물
- 배경
- 조명과 분위기
- Veo용 영어 프롬프트
- 영상 길이: 계정에 실제 노출된 선택지만 사용하고 미노출이면 명시
- 장면 연결 방법

반복 등장하는 인물의 외모와 의상 설명을 동일하게 유지한다. 색감, 시대, 카메라 스타일, 분위기, 장소의 연속성을 유지한다. 필요하면 첫 장면의 결과를 후속 장면 참고 자료로 사용하되 실제 지원 여부를 확인한다.

## 3. 테스트 영상 승인 및 생성

- 처음에는 가장 중요한 장면 하나만 선택한다. 기본 화면 비율은 16:9이며 계정 지원 여부를 확인한다.
- 실행 전에 실제 사용할 모델, 영상 길이 또는 길이 선택 불가 상태, 비율, 생성 개수(1개), 사용할 Flow 크레딧을 알린다.
- 정확한 크레딧 수치가 도구에 노출되지 않으면 확인할 수 없다고 명시하고 추정치를 확정 비용처럼 제시하지 않는다.
- 위 조건을 제시한 후 사용자의 명확한 생성 승인을 받는다. 프로젝트 목표나 대본 승인만으로 크레딧 사용을 승인받았다고 해석하지 않는다.
- 승인 후 Google Flow MCP로 영상 1개를 생성한다. 업스케일은 별도 승인 없이 실행하지 않는다.
- 반환된 Job ID를 즉시 기록한다. 처리 중이면 같은 Job ID로 상태를 확인하며 같은 프롬프트를 재제출하지 않는다.
- 완료 후 해당 Job ID의 결과만 다운로드하고 파일 존재, 크기 및 가능한 미디어 정보를 확인한다.
- 테스트 파일은 `<영상 제목 폴더>/scene-01/`에 저장한다. 테스트에 고른 원래 대본 장면이 다르면 로그에 대응 관계를 명시한다.

## 4. 후속 장면

- 사용자가 테스트 결과를 확인하고 계속 진행하도록 승인하면 승인 범위 안에서 나머지 장면을 순서대로 생성한다.
- 조건·생성 개수·비용 범위가 변경되면 실행 전에 알리고 승인을 받는다.
- 각 영상은 `<영상 제목 폴더>/scene-02/`, `<영상 제목 폴더>/scene-03/` 등 장면별 폴더에 저장하고 실제 프롬프트도 함께 저장한다.
- 기존 결과를 임의로 덮어쓰거나 다른 갤러리 결과로 대체하지 않는다.

## 영상 제목별 저장 폴더

- 다운로드할 완성 영상의 제목별로 프로젝트 루트 바로 아래에 폴더를 만든다. 기본 구조는 `<프로젝트 루트>/<영상 제목>/scene-01/`이다.
- 같은 영상에 속한 모든 장면 클립은 같은 제목 폴더 아래의 `scene-01/`, `scene-02/` 등으로 모은다. 다운로드할 때마다 같은 제목의 폴더를 중복 생성하지 않는다.
- 영상 제목이 정해지면 최초 생성 요청 전에 제목 폴더와 장면 폴더를 만들고, 해당 장면의 절대 경로를 Flow MCP의 `outputDirectory`로 전달한다. 다운로드는 그 Job에 저장된 경로를 따른다.
- Windows에서 사용할 수 없는 문자(`< > : " / \ | ? *` 및 제어 문자)는 `_`로 바꾸고 끝의 공백·마침표를 제거한다. 한글 제목은 유지하며 예약 이름이나 빈 이름은 안전한 폴더명으로 바꾸고 원래 제목과 실제 폴더명을 기록한다.
- 서로 다른 영상의 제목이 같거나 폴더명 변환으로 충돌하면 새 영상에 `(2)`, `(3)` 등의 구분자를 붙인다. 기존 영상의 파일을 덮어쓰지 않는다.
- 제목별 `script.md`, `scenes.md`, `veo-prompts.md`, `generation-log.md`도 해당 제목 폴더에 함께 저장한다. 현재 루트의 미작성 문서는 작성 양식이며 실제 영상 작업 기록은 제목별로 관리한다.
- 아직 제목이나 생성 결과가 없으면 임의 제목의 폴더나 가짜 다운로드 파일을 만들지 않는다.

## 산출물

- `script.md`: 전체 YouTube 대본
- `scenes.md`: 장면별 설명과 내레이션
- `veo-prompts.md`: 실제 사용할/사용한 영어 프롬프트와 제출 여부
- `generation-log.md`: 장면, 모델, 요청 길이와 확인 가능한 실제 길이, 화면 비율, 출력 개수, 생성 날짜, 승인 범위, 확인 가능한 크레딧 정보, Job ID, 작업 상태, 파일 위치

실제 작성·승인·생성 상태를 구분하며 실행하지 않은 작업을 완료로 기록하지 않는다.

## 오류 및 제한

CAPTCHA, Google 인증, 지역 제한, Flow 제한을 우회하지 않는다. 비밀번호나 2FA 코드를 요청·저장하지 않는다. 오류가 발생하면 정확한 내용을 사용자에게 알리고 임의의 다른 경로로 우회하지 않는다. MCP 설치 저장소 소스를 수정하거나 임의 브라우저 플래그 또는 원격 디버깅을 추가하지 않는다.

## 개발 복사본 수정 승인 — 2026-09-10

사용자는 "개발 복사본 수정해"라고 명시 승인했다. 프로젝트 안 `tools/google-flow-mcp/` 개발 복사본의 현재 Flow UI 대응, 생성 설정 코드, 도구 안내 및 관련 테스트 수정을 허용한다. 원본 설치 저장소 `C:/Users/kikuke/AppData/Local/google-flow-mcp`의 소스 수정 금지는 유지한다. 이 승인은 코드 수정·검증 범위이며 실제 영상 생성, 업스케일 또는 크레딧 사용 승인은 포함하지 않는다. 기존 대본 승인·생성 조건 제시·명시적 생성 승인 단계는 그대로 적용한다.

## 내레이션 범위 추가 — 2026-09-10

사용자는 롯데월드타워 영상에 내레이션을 넣고 신뢰감 있는 여성 아나운서 톤을 사용하도록 요청했다. 이 영상은 한국어 내레이션 대본, 동일한 여성 목소리의 음성 제작, 영상에 내레이션을 넣는 데 필요한 처리를 범위에 포함한다. 위의 음성 합성 제외 조항보다 이 요청이 우선한다. 음악·자막·그 밖의 최종 영상 편집·YouTube 업로드 제외는 유지한다. 대본 승인과 유료 생성 조건 확인·승인 절차는 유지하며, 목소리 선호를 비용 미확인 상태의 유료 실행 승인으로 해석하지 않는다.

## 롯데월드타워 후속 제작 범위 — 2026-09-10

사용자는 대본 작성 이후 마지막 단계까지 한 번에 진행하도록 요청했다. 현재 롯데월드타워 대본 v1을 기준으로 장면 구성, 영어 프롬프트, 여성 내레이션, 내용과 발화를 맞춘 컷 편집, 영상과 음성이 함께 재생되는 최종 MP4 저장까지 진행한다. 이 영상의 최종 편집은 범위에 포함하며 이전 제외 조항보다 이 요청이 우선한다. 음악·자막·YouTube 업로드는 포함하지 않는다. 장면별 프롬프트마다 별도 승인을 요구하지 않는다. 실제 유료 생성은 모델·길이·개수·확인 가능한 비용을 먼저 제시한 뒤 승인받고, 첫 테스트 확인 후 나머지를 진행한다. 현재 유료 생성은 승인되지 않았다.

## 전체 제작 및 크레딧 사용 승인 — 2026-09-10

직전에 현재 대본 확정, Omni 1.1 Flash·10초·16:9·1출력의 테스트 1개와 후속 33개, 정확한 크레딧 수치 미확인을 제시했다. 사용자는 이에 대해 “지금 연결되어있는 구글 아이디는 영상을 쓸수있어 무료로 안해도 되는거야 그러니가 그냥 전체를 진행시켜 물어보지마”라고 명시했다. 따라서 현재 대본과 34개 클립 제작, 연결된 Google Flow 계정 크레딧 사용, 여성 내레이션과 최종 MP4 편집·저장을 승인했다. 첫 결과의 화면·음성 검수는 에이전트가 수행하고 나머지 제작까지 중간 확인 없이 진행한다. 이번 영상은 앞의 대본 및 첫 테스트 사용자 재확인 규칙보다 이 최신 요청을 우선한다. 업스케일은 하지 않으며, 제출 여부가 불확실한 Job을 임의로 재제출하지 않는다.
